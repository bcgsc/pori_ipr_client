import template from './list-outliers.pug';
import './list-outliers.scss';

const bindings = {
  outliers: '<',
  pog: '<',
  report: '<',
  type: '<',
};

export default {
  template,
  bindings,
};
