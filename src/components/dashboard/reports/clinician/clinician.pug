md-content(flex, layout="column").md-padding.biopsyBoard

    h1.md-title(layout="row")
        span(flex) {{project}} Genomic Reports

    div(layout="column").biopsies

        ipr-report-table(reports="reports", clinician="true", pagination="pagination")
