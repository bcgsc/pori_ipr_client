import React from 'react';
import LaunchIcon from '@material-ui/icons/Launch';

import './index.scss';

const LaunchCell = (): JSX.Element => (
  <LaunchIcon className="launch-cell" color="action" />
);

export default LaunchCell;
