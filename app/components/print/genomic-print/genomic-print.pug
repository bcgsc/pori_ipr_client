genomicsummary(
  pog="$ctrl.pog",
  report="$ctrl.report",
  genomic-alterations="$ctrl.genomicAlterations",
  variant-counts="$ctrl.variantCounts",
  mutation-summary="$ctrl.mutationSummary",
  probe-target="$ctrl.probeTarget",
  mutation-signature="$ctrl.mutationSignature",
  microbial="$ctrl.microbial",
  print="true",
)

br

print-header(pog="$ctrl.pog", report="$ctrl.report")

analyst(
  analyst-comments="$ctrl.analystComments",
  print="true"
)

print-header(pog="$ctrl.pog", report="$ctrl.report")

pathway(
  pathway="$ctrl.pathway",
  pog="$ctrl.pog",
  report="$ctrl.report",
  print="true",
)

print-header(pog="$ctrl.pog", report="$ctrl.report")

therapeutic-react(
  therapeutic-row-data="$ctrl.therapeuticRowData",
)

print-header(pog="$ctrl.pog", report="$ctrl.report")

slides(
  pog="$ctrl.pog",
  report="$ctrl.report",
  slides="$ctrl.slides",
  print="true",
)
